<h1></h1>

<table width='80%'>
  <tr>
    <th>Dentist</th>
    <th>Address</th>
    <th>Email</th>
    <th>City</th>
    <th>State</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dentists.each do |dentist| %>
  <tr>
    <td><%= link_to dentist.last + ', ' + dentist.first + ' ' + dentist.middle + 
      ', ' + dentist.title, dentist %></td>
    <td><%= dentist.address1 %></td>
    <td><%= dentist.email %></td>
    <td><%= dentist.city %></td>
    <td><%= dentist.state %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dentist', new_dentist_path %>
